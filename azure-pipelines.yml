# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

language: python
python:
  - "3.7"
  - "2.7"
install:
  - pip install -U -r py.requirements/ci.travis.txt --use-mirrors
script: 
 - cd calculator
 - behave
